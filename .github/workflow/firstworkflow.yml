name: Burani and Immy
run-name: Testing Github Actions
on : [push]
jobs:
  Test-GithunActions:
    runs-on: ubuntu-latest
    steps:
    - run : echo "make the dough"
    - run : echo "make rounds"
    - name: Checkout code from repo
      uses: actions/checkout@v2
    - run : echo "add sugar"
    - run : echo "add blueband"
    - name: List files above
    run: 
       ls ${{github.workspace}}
    - run : echo "Group memebers : Burani and Immy"
    
    